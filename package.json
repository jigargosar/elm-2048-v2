{
  "scripts": {
    "reactor": "elm reactor",
    "start": "parcel index.html --open",
    "start:no-debug": "set PARCEL_ELM_NO_DEBUG=1 && parcel index.html --open",
    "build:gh-pages": "parcel build --dist-dir docs --no-optimize --no-source-maps --no-content-hash index.html"
  },
  "browserslist": ["defaults and supports es6-module and supports css-grid"],
  "devDependencies": {
    "@parcel/transformer-elm": "^2.8.0",
    "elm-tooling": "^1.10.0",
    "parcel": "^2.8.0"
  },
  "dependencies": {
    "lit-element": "^3.2.2"
  }
}
